<Window x:Class="KursovayaRabotaApp.AddBookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AddBookWindow" Height="200" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Content="Введите название книги" Grid.Row="0" Margin="0,0,0,5"/>
        <TextBox Name="titleTextBox" Grid.Row="1" Width="200" Margin="0,0,0,10"/>

        <Label Content="Введите тираж" Grid.Row="2" Margin="0,0,0,5"/>
        <TextBox Name="circulationTextBox" Grid.Row="3" Width="200" Margin="0,0,0,10"/>

        <DockPanel Grid.Row="4" LastChildFill="False">
            <Button Name="addButton" Content="Добавить" Margin="10,0,0,0" Width="75" Height="25" DockPanel.Dock="Left"/>
            <Button Name="cancelButton" Content="Отмена" Margin="0,0,10,0" Width="75" Height="25" DockPanel.Dock="Right"/>
        </DockPanel>
    </Grid>
</Window>
